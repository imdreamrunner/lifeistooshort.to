language: node_js

node_js:
  - "10"

install:
  - git clone git@github.com:imdreamrunner/litst.git
  - cd litst
  - npm install
  - npm link
  -- cd ..

script:
  - litst publish ./pages --output ./publish


deploy:
  provider: pages
  local-dir: publish
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  on:
    branch: master
